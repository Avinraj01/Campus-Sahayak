// This script would normally clear test users from the database
// Since we don't have direct database access, we'll just log a message
console.log('To clear test users, you would need to:');
console.log('1. Access the MongoDB database directly');
console.log('2. Remove test users from the users collection');
console.log('3. Or use a different email address for testing');